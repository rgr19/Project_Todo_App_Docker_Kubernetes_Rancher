apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: ${NAMESPACE}
  name: elastic-data-pvc
spec:
#  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: ${ELASTIC_PV_CAPACITY_STORAGE}
