apiVersion: v1
kind: PersistentVolume
metadata:
  name: elastic-data-pv
  labels:
    type: local
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: ${ELASTIC_PV_CAPACITY_STORAGE}
  hostPath:
    path: ${ELASTIC_PV_HOST_PATH}
